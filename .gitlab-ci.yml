stages:
  - build

variables:
  BUILD_CONFIG: 'Debug'

build:
  stage: build
  tags:
    - windows

  script:
    - ./msvc_build.bat
    - move ab/ab/%BUILD_CONFIG%
  artifacts:
    paths:
      - %BUILD_CONFIG%/*
